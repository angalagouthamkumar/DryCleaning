# Workspace Rules & Pending Tasks

## Order Status Notifications Architecture
- When the user says `execute notification` or `execute notifications`, execute the implementation of the Order Status Notifications System (FCM + local notifications across backend and Flutter customer app for all 15 order progress stages).
- Do not make any code changes until the explicit phrase `execute notification` is given.
