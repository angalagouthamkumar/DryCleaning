abstract class AppConstants {
  static const String appName = 'DryClean Store';
  static const double minOrderAmount = 200.0;
  static const double freeDeliveryThreshold = 500.0;
  static const double defaultDeliveryFee = 40.0;

  static const Duration splashDuration = Duration(milliseconds: 2000);
  static const Duration animationDurationFast = Duration(milliseconds: 150);
  static const Duration animationDurationNormal = Duration(milliseconds: 300);
  static const Duration animationDurationSlow = Duration(milliseconds: 500);

  static const double defaultLatitude = 17.4486;
  static const double defaultLongitude = 78.3908;
}
